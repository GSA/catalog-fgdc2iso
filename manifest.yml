---
applications:
- name: ((app_name))
  services:
    - ((app_name))-secrets
  buildpacks:
    - java_buildpack
  # Idle sits at 215M, may need to bump for high usage
  memory: 256M
  routes: ((routes))
  instances: ((instances))
  health-check-type: port
  disk_quota: 512M
  timeout: 180