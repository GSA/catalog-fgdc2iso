---
applications:
- name: ((app_name))
  services:
    - ((app_name))-secrets
  buildpacks:
    - java_buildpack
  memory: 0.75G
  routes: ((routes))
  # no-route: true
  instances: ((instances))
  health-check-type: port
  disk_quota: 512M
  timeout: 180